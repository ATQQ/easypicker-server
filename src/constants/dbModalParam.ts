export enum CourseType {
    PARENT = 1,
    CHILD = 0
}

export enum CourseListType {
    PARENT = 2,
    CHILDREN = 3
}

export enum childContentType {
    DDL = 1,
    PEOPLE = 2,
    TEMPLATE = 3
}